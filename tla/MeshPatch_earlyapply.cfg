\* Broken controller: broadcast APPLY after any single READY (RequireAllReady
\* = FALSE). EXPECTED: ApplySafety VIOLATED - APPLY goes out while some
\* expected satellite has not reported READY. Validates the all-ready gate (#6a).
SPECIFICATION Spec
CONSTANTS
    Sats = {1, 2}
    NumBlocks = 2
    RequireAllReady = FALSE
    CheckFullHash = TRUE
    AllowStaleBlocks = FALSE
INVARIANT TypeOK
INVARIANT ApplySafety
