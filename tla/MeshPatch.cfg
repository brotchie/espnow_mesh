\* Shipped patch distribution, benign fair channel (no stale blocks).
\* EXPECTED: all pass - apply-safety (#6a), apply-integrity/agreement (#6b),
\* and progress to READY and APPLY under fair delivery (#11).
SPECIFICATION FairSpec
CONSTANTS
    Sats = {1, 2}
    NumBlocks = 2
    RequireAllReady = TRUE
    CheckFullHash = TRUE
    AllowStaleBlocks = FALSE
INVARIANT TypeOK
INVARIANT ApplySafety
INVARIANT ApplyIntegrity
PROPERTY PatchProgress
PROPERTY ApplyProgress
