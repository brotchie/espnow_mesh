\* Broken controller check (EnforceLock = FALSE): the satellite acts on any
\* controller's frame regardless of its lock or discovery state, with two
\* controllers present. EXPECTED: LockIntegrity VIOLATED - the satellite
\* flaps between two controllers / acts before discovery. Validates that the
\* handle_satellite_data controller + SoftAP checks are load-bearing (#3).
SPECIFICATION Spec
CONSTANTS
    Ctls = {1, 2}
    Channels = {1}
    MaxBoot = 1
    SoftApRequired = TRUE
    EnforceLock = FALSE
INVARIANT TypeOK
INVARIANT LockIntegrity
