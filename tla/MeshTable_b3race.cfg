\* The B3 race: registration insert runs off the mesh task, so check and
\* insert can interleave with the ACK path. EXPECTED: TableConservation
\* VIOLATED - both paths observe the MAC absent and both append it, producing
\* two entries for one MAC. Shows the model catches the pre-fix bug.
SPECIFICATION Spec
CONSTANTS
    Macs = {1, 2}
    Serialized = FALSE
INVARIANT TableConservation
