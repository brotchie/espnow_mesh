\* Proposed hardening "require k consecutive frames of a new boot_id" vs. the
\* replay adversary. EXPECTED: DedupSoundness VIOLATED - a per-frame
\* consecutive counter does not help, because the adversary simply replays
\* the SAME recorded frame KConsec times.
SPECIFICATION Spec
CONSTANTS
    MaxBoot = 2
    MaxSeq = 2
    Channels = {1, 2}
    InitChannel = 1
    SwitchRule = "kconsec"
    KConsec = 2
    ReplayAdversary = TRUE
INVARIANT TypeOK
INVARIANT DedupSoundness
